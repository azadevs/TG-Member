<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="4dp"
    android:paddingVertical="6dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/ivChannelImage"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/msg_openprofile"
        android:tint="@color/widget_time"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearance="@style/CircleShapeableImageView" />

    <TextView
        android:id="@+id/tvChannelName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:fontFamily="@font/poppins_medium"
        android:includeFontPadding="false"
        android:textSize="15sp"
        app:layout_constraintBottom_toBottomOf="@id/ivChannelImage"
        app:layout_constraintBottom_toTopOf="@id/tvChannelUserName"
        app:layout_constraintStart_toEndOf="@id/ivChannelImage"
        app:layout_constraintTop_toTopOf="@id/ivChannelImage"
        tools:text="@tools:sample/full_names" />

    <TextView
        android:id="@+id/tvChannelUserName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/poppins_regular"
        android:textSize="13sp"
        app:layout_constraintBottom_toBottomOf="@id/ivChannelImage"
        app:layout_constraintStart_toStartOf="@id/tvChannelName"
        app:layout_constraintTop_toBottomOf="@id/tvChannelName"
        tools:text="@tools:sample/full_names" />


</androidx.constraintlayout.widget.ConstraintLayout>